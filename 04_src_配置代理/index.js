import React from 'react';
import ReactDOM from 'react-dom/client';  // 后面/client一定要加
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);
